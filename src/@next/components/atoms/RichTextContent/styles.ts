import { styled } from "@styles";

export const Wrapper = styled.div`
  ul,
  ol {
    list-style: inherit;
    padding-left: 2rem;
    padding-top: 1rem;
  }
  h2 {
    display: block;
    font-size: 1.5em;
    margin: 0.8 rem;
    font-weight: bold;
  }
`;
